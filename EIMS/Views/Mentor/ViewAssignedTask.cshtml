@model IEnumerable<EIMS.Models.ProjectTaskModel>

@{
    ViewBag.Title = "ViewAssignedTask";
}

<h2>ViewAssignedTask</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.prefId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.weekId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.prefId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.weekId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
